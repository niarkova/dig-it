import { CmToInchesPipe } from './cm-to-inches.pipe';

describe('CmToInchesPipe', () => {
  it('create an instance', () => {
    const pipe = new CmToInchesPipe();
    expect(pipe).toBeTruthy();
  });
});
